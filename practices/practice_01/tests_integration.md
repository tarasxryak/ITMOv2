# Integration-проверки

| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| I-01 SEC-1: API ↔ ReviewService ↔ LLM | Секрет уходит в LLM | POST /api/reviews с тестовым секретом; перехват prompt | В prompt [REDACTED], исходного секрета нет | SEC-1; review_service.py:14 |
| I-02 API-1: API ↔ LLM | Превышение лимита | POST с diff длиной 20001 | 413; вызов LLM = 0 | API-1; api.py:10 |
| I-03 REL-1: ReviewService ↔ LLM | Таймаут | мок LLM отвечает >10s | HTTP 504 (допущение) и тело OUT-1: summary об ошибке, пустые risks и checks; не 500 | REL-1; review_service.py:15 |
| I-04 OBS-1: API ↔ Логи | Логи утекли | POST /api/reviews с diff, содержащим OBS_MARKER_7f3a; заглушка LLM отвечает RESP_MARKER_91c2; перехватить логи | Ни OBS_MARKER_7f3a, ни RESP_MARKER_91c2 в логах нет; ключи записи лога только {request_id, duration, status} | OBS-1 |
| I-05 QA-1/OUT-1: Formatter | Лишние/неподтвержденные риски | LLM возвращает 4 риска, один без evidence | Ответ содержит ≤3 подтвержденных риска и соответствует OUT-1 | QA-1, OUT-1 |
| I-06 SCOPE-1: API ↔ Внешние действия | LLM ответил "approve" или прислал патч | POST /api/reviews; LLM-заглушка возвращает текст с инструкциями | В ответе только поля OUT-1; моки внешних клиентов вызваны 0 раз; patch/approve из ответа LLM не исполняются | SCOPE-1 |

## Как использовали AI

- Для чего: определить интеграционные проверки на связях.
- Тип промпта: master prompt.
- Строка в [`prompts.md`](prompts.md): P1-03 (генерация артефактов), P1-04 (доработка по ревью), P1-07 (аудит готовности).
- Что проверили и исправили сами: убрали 422 и привязку QA-1 к валидации входа; I-04 перевели на маркеры OBS_MARKER_7f3a и RESP_MARKER_91c2 и проверку ключей записи лога; I-06 сделали проверяемым (моки внешних клиентов вызваны 0 раз); в I-03 добавили проверку тела OUT-1; у I-02 проверяется нулевой вызов LLM.
