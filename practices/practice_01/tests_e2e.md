# E2E-проверки

| Сценарий пользователя | Предусловия | Действие | Наблюдаемый результат | Evidence |
|---|---|---|---|---|
| E-01 TRAINING_PR.diff → OUT-1 | API доступен | POST /api/reviews с содержимым TRAINING_PR.diff | 200; тело соответствует OUT-1; file:line каждого риска существует в diff | OUT-1 |
| E-02 SEC-1 маскировка | API доступен, LLM-заглушка перехватывает prompt | POST /api/reviews с diff, содержащим тестовый секрет | в prompt — [REDACTED]; в ответе и логах исходного секрета нет | SEC-1, OBS-1 |
| E-03 Лимит размера | API доступен | POST /api/reviews с diff длиной 20001 | 413 Payload Too Large; вызовов LLM = 0 | API-1 |
| E-04 Таймаут LLM | API доступен, LLM-заглушка задерживает ответ >10s | POST /api/reviews с валидным diff | HTTP 504 (допущение) и тело OUT-1: summary об ошибке, пустые risks и checks; не 500 | REL-1 |
| E-05 Граничный 20000 | API доступен | POST /api/reviews с diff длиной 20000 | 200; тело соответствует OUT-1 | API-1, OUT-1 |

## Как использовали AI

- Для чего: сформировать сквозные сценарии от лица ревьюера.
- Тип промпта: master prompt.
- Строка в [`prompts.md`](prompts.md): P1-03 (генерация артефактов), P1-04 (доработка по ревью), P1-07 (аудит готовности).
- Что проверили и исправили сами: добавили E-05 (граница 20 000 → 200) и нулевой вызов LLM в E-03; в E-02 убрали требование [REDACTED] в ответе LLM — проверяется отсутствие секрета в ответе и логах; в E-04 добавили проверку тела OUT-1; сценарии сверили с TRAINING_PR.diff и правилами.
