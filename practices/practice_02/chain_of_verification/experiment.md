# Chain of Verification

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Проверяемый черновик или утверждение:
- Три ячейки «Текущее значение или способ замера» в таблице «Метрики» файла practices/practice_01/problem.md:
  - SEC-1: «По коду: 100% уходят в LLM (review_service.py:14)»
  - OUT-1: «По коду: 0% (review_service.py:16)»
  - REL-1: «По коду: 100% (review_service.py:15)»
  - И ячейка «Целевое изменение» REL-1: «0; timeout → 504 с телом OUT-1»

## Запрос на проверку

Проверить по practices/practice_01/TRAINING_PR.diff и связанным артефактам корректность ссылок на строки и пометку допущения для 504, затем зафиксировать исправленный вид четырёх ячеек.

## Вопросы и результаты проверки

| Вопрос | Источник или проверка | Результат |
|---|---|---|
| 1. Действительно ли весь diff без маскировки попадает в prompt? Какая строка диффа это подтверждает? | TRAINING_PR.diff:20 | Да. TRAINING_PR.diff:20 — prompt = f"Review this pull request and find problems:\n{diff}" |
| 2. Какой номер строки в app/review_service.py после применения патча соответствует подстановке diff в prompt? | TRAINING_PR.diff, хунк @@ -1,10 +1,23 @@; пересчёт по новому файлу | 14 |
| 3. Какой номер строки после патча соответствует вызову llm.generate без обработки ошибок и таймаута? | TRAINING_PR.diff, хунк @@ -1,10 +1,23 @@ | 15 |
| 4. Какой номер строки после патча соответствует возврату свободного формата {"comment": answer}? | TRAINING_PR.diff, хунк @@ -1,10 +1,23 @@ | 16 |
| 5. Помечено ли в problem.md значение 504 как допущение? Сравнение с adr.md и tests_e2e.md | problem.md:20; adr.md:16 — «Допущение: HTTP 504…»; tests_e2e.md:8 — «HTTP 504 (допущение)» | В problem.md не помечено как допущение; в adr.md и tests_e2e.md явно помечено |

## Исправленный результат

- SEC-1 (Текущее значение или способ замера): По коду: 100% уходят в LLM (review_service.py:14)
- OUT-1 (Текущее значение или способ замера): По коду: 0% (review_service.py:16)
- REL-1 (Текущее значение или способ замера): По коду: 100% (review_service.py:15)
- REL-1 (Целевое изменение): 0; timeout → 504 (допущение) с телом OUT-1

## Что изменили в исходном артефакте

- Файл и раздел: practices/practice_01/problem.md, таблица «Метрики» (строка REL-1) и раздел «Почему изменение метрики подтвердит решение проблемы»
- Изменение: HTTP 504 помечен как допущение в обоих местах; ссылки review_service.py:14, :15, :16 оставлены без изменений — проверка подтвердила их
- Что отклонили: правку ссылок на строки — пересчёт по хунку @@ -1,10 +1,23 @@ подтвердил, что подстановка diff в prompt это строка 14, вызов llm.generate — 15, возврат {"comment": answer} — 16; менять было нечего

Скрытые рассуждения модели не сохраняйте; нужны только вопросы, подтверждения и исправленный результат.
